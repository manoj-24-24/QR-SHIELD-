# QR Shield

Deploy-ready Vite + React PWA for safe QR scanning.

## Render

Use these settings:

- Build command: `npm install && npm run build`
- Publish directory: `dist`

The app is installable after deployment over HTTPS.
